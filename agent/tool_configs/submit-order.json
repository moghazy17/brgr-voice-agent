{
  "name": "submit_order",
  "description": "Finalize the demo order after the customer confirms the items. Do not collect name, phone, address, or payment details. Do not mention prices unless asked. Returns an order number.",
  "type": "webhook",
  "method": "POST",
  "url": "https://brgr-tools.vercel.app/api/tools/submit-order",
  "parameters": {
    "type": "object",
    "properties": {
      "conversationId": {
        "type": "string",
        "description": "The ElevenLabs conversation ID for this call."
      }
    },
    "required": [
      "conversationId"
    ],
    "description": "Object parameters."
  }
}
